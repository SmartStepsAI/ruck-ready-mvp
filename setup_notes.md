# Setup Notes for Operator or Dev

1. Ensure Firebase project is created
2. Replace `your-firebase-project-id` in `.firebaserc` with your actual project ID
3. Run the commands in README to deploy
4. Customize components inside `/src/components` as needed
